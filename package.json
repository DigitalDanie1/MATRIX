{
  "name": "matrix",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html"
  },
  "dependencies": {
    "yahoo-finance2": "^3.14.0"
  }
}
